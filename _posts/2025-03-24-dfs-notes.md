---
layout: post
categories: [Algorithms]
title: "DFS：从算法第四版到 LeetCode"
---

## 一、DFS 是什么
一句话：一条路走到黑，走不动就回头。

## 二、算法第四版里的 DFS
BST 的 get/put、本质都是递归 DFS。

## 三、LeetCode 模板
```java
void dfs(Node x) {
    if (x == null) return;
}

```